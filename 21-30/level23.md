
Goal: A program is running automatically at regular intervals from cron, the time-based job scheduler. Look in /etc/cron.d/ for the configuration and see what command is being executed.

Commands used:
  - ```ls```.
  - ```cd```.
  - ```nano```.
  - ```rm```.

Procedure:
  - changing working directory to the one specified, I tried to read the script. This script proved to be a little more challenging to read.
  - As suggested, I decided to write my own script which took a lot more time than I anticipated.
  - Understanding the script given, the one I wrote did yield the password after a lot of errors and syntax learnings.

Learnings:
  - Syntax in bash
  - Writing a script (woohoo!)

  
