
Goal: To retrieve the password stored in the file data.txt in one of the few human-readable strings, preceded by several ‘=’ characters.

Commands used:
  - ```ls -a```, to list out all folders and directories.
  - ```strings```, to read ```data.txt``` in human readable format.

Procedure:
  - I looked into the syntax for using ```strings```, and simply adding the filename against the command gave the output of the entire file in human readable format

Learnings:
  - Purpose and usage of ```string```.

Conclusion: Completed Level 09 successfully.
