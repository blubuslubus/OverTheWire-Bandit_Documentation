
Goal: To find the password stored in the file data.txt next to the word millionth.

Commands Used:
  - ```ls -a``` to list all files and directories.
  - ```grep``` to efficiently search through the text file.

Procedure:
  - After locating the ```data.txt``` file, I used the grep command with its syntax to find for the word (or pattern) "millionth".

Learnings:
  - Purpose, usage, and syntax of grep function including flags.

Conclusion:
  - Completed level 07 successfully.
