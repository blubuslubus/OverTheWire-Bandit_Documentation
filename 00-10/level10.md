
Goal: To retrieve the password stored in the file data.txt, which contains base64 encoded data.

Commands used:
  - ```ls -a```, to list out all files and directories.
  - ```base64 -d```, to decode the encoded file ```data.txt```.

Procedure:
  - Looking up the relevant syntax and flags for the ```base64``` command, the password became simple to retrieve by decoding ```data.txt```

Learnings:
  - Purpose and usage of ```base64``` and some of its flags.

Conclusion: Completed Level 10 successfully.
